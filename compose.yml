services:
  app:
    build: .
    ports:
      - "6767:6767"
    env_file:
      - .env
    environment:
      - PORT=6767
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
      - CHAT_MODEL=${CHAT_MODEL}
